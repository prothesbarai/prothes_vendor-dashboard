import 'package:flutter/material.dart';

class PublishItemsPage extends StatelessWidget {
  const PublishItemsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Publish Items")),
    );
  }
}
